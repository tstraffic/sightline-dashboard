<%
  // T&S logo for VOC forms + (Phase 2) certificate.
  // `size` local controls the rendered height in px (default 70).
  const _size = (typeof size !== 'undefined' && size) ? size : 70;
%>
<img src="/images/logo.jpg" alt="T&amp;S Traffic Control" style="height:<%= _size %>px; width:auto; display:block; object-fit:contain;">
