<% locals.currentPage = 'safety-quizzes' %>
<%
  function fmtDate(s) {
    if (!s) return '—';
    return new Date(s).toLocaleDateString('en-AU', { timeZone: 'Australia/Sydney', day: 'numeric', month: 'short', year: 'numeric' });
  }
%>

<div class="max-w-6xl mx-auto space-y-5">
  <div class="flex items-start justify-between gap-3 flex-wrap">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">Safety Quizzes</h1>
      <p class="text-sm text-gray-500 mt-0.5">Knowledge checks for toolboxes, SWMS, and general safety topics. Workers see published quizzes in their portal.</p>
    </div>
    <a href="/safety-quizzes/new" class="px-3 py-2 bg-brand-600 hover:bg-brand-500 text-white text-sm font-semibold rounded-lg inline-flex items-center gap-1.5">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v12m6-6H6"/></svg>
      New quiz
    </a>
  </div>

  <!-- Status tabs -->
  <div class="border-b border-gray-200 flex items-center gap-1">
    <% ['published','draft','archived'].forEach(s => { %>
      <a href="/safety-quizzes?status=<%= s %>" class="px-4 py-2 -mb-px border-b-2 text-sm font-semibold <%= status === s ? 'border-brand-600 text-brand-700' : 'border-transparent text-gray-500 hover:text-gray-700' %>">
        <%= statusLabels[s] %>
        <span class="ml-1 px-1.5 py-0.5 text-[10px] rounded-full <%= status === s ? 'bg-brand-50 text-brand-700' : 'bg-gray-100 text-gray-500' %>"><%= counts[s] || 0 %></span>
      </a>
    <% }) %>
  </div>

  <% if (!rows.length) { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm px-5 py-10 text-center text-sm text-gray-500">
      No <%= statusLabels[status].toLowerCase() %> quizzes.
    </div>
  <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
      <table class="min-w-full text-sm">
        <thead class="bg-gray-50 border-b border-gray-200">
          <tr>
            <th class="px-3 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Title</th>
            <th class="px-2 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Questions</th>
            <th class="px-2 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Mandatory</th>
            <th class="px-2 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Pass mark</th>
            <th class="px-2 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Workers passed</th>
            <th class="px-2 py-2 text-left text-[10px] font-bold text-gray-500 uppercase tracking-widest">Deadline</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-100">
          <% rows.forEach(r => { %>
            <tr class="hover:bg-gray-50/50">
              <td class="px-3 py-2">
                <a href="/safety-quizzes/<%= r.id %>" class="font-semibold text-gray-900 hover:text-brand-600"><%= r.title %></a>
                <% if (r.source_type) { %><p class="text-xs text-gray-400 mt-0.5">Linked to <%= r.source_type %></p><% } %>
              </td>
              <td class="px-2 py-2 text-xs text-gray-600"><%= r.question_count %></td>
              <td class="px-2 py-2 text-xs"><% if (r.is_mandatory) { %><span class="text-rose-600">Mandatory</span><% } else { %><span class="text-gray-400">Optional</span><% } %></td>
              <td class="px-2 py-2 text-xs text-gray-600"><%= r.pass_mark %>%</td>
              <td class="px-2 py-2 text-xs text-gray-600"><%= r.passed_workers %></td>
              <td class="px-2 py-2 text-xs text-gray-600 tabular-nums"><%= fmtDate(r.deadline_at) %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>
