<div style="padding:0.5rem;">
  <a href="/w/forms" class="inline-flex items-center gap-1 text-sm font-semibold mb-3" style="color:#10B981;">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.4"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/></svg>
    Forms
  </a>
  <h1 class="text-xl font-bold" style="color:#fff;">Custom checklists</h1>
  <p class="text-sm mt-1" style="color:rgba(255,255,255,0.55);">Forms published by the office. Submissions are versioned to the revision live at the time you fill them in.</p>

  <% if (!templates || templates.length === 0) { %>
    <div class="mt-5 rounded-xl p-6 text-center" style="background:rgba(255,255,255,0.03);border:1px dashed rgba(255,255,255,0.10);">
      <p style="font-size:0.92rem;color:rgba(255,255,255,0.7);font-weight:600;">No custom checklists yet</p>
      <p class="mt-1" style="font-size:0.78rem;color:rgba(255,255,255,0.45);">When the office publishes one it'll appear here.</p>
    </div>
  <% } else { %>
    <div class="mt-4 space-y-2">
      <% templates.forEach(function (t) { %>
        <a href="/w/forms/custom/<%= t.id %>" class="flex items-center justify-between py-3 px-4 rounded-xl fc-item"
           style="text-decoration:none;">
          <div style="min-width:0;">
            <p style="font-size:0.92rem;font-weight:700;color:#fff;line-height:1.2;"><%= t.name %></p>
            <% if (t.description) { %>
              <p style="font-size:0.72rem;color:rgba(255,255,255,0.5);margin-top:2px;"><%= t.description %></p>
            <% } %>
            <div style="display:flex;gap:6px;margin-top:6px;flex-wrap:wrap;">
              <span class="fc-badge--rev" style="font-size:0.6rem;font-weight:700;letter-spacing:0.06em;text-transform:uppercase;padding:2px 7px;border-radius:999px;">Rev <%= t.published_revision %></span>
              <% if (t.require_signature) { %><span class="fc-badge--sig" style="font-size:0.6rem;font-weight:700;letter-spacing:0.06em;text-transform:uppercase;padding:2px 7px;border-radius:999px;">Signature</span><% } %>
              <% if (t.my_submissions) { %><span style="font-size:0.6rem;font-weight:600;color:rgba(255,255,255,0.45);">&middot; <%= t.my_submissions %> filed by you</span><% } %>
            </div>
          </div>
          <svg class="w-4 h-4 shrink-0" style="color:rgba(255,255,255,0.4);" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
        </a>
      <% }) %>
    </div>
  <% } %>
</div>
